function myString(input) {
  let temp = new Array[input]();
  let result = temp.join("");
  return result;

  function length(input) {
    let result = new Array[input]();
    return result.length;
  }

  function charAt(index) {
    return temp[index];
  }
}
